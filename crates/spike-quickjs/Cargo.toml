[package]
name = "spike-quickjs"
version = "0.1.0"
edition = "2021"

[dependencies]
rquickjs = { version = "0.11", features = ["macro", "loader"] }

[dev-dependencies]
reqwest = { version = "0.12", features = ["blocking"] }
url = "2.5"
