[package]
name = "test-runner"
version = "0.1.0"
edition = "2021"

[dependencies]
braille-engine = { path = "../engine" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[lints]
workspace = true
